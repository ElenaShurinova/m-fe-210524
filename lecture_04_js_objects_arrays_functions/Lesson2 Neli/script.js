// 1. Написать функцию multNumber, которая принимает число в качестве аргумента и возвращает это число, умноженное на 2

const multNumber = (num) => {
    return num * 2
}

const num1 = multNumber(2); // 4
const num2 = multNumber(5); // 10

console.log(num1);
console.log(num2);